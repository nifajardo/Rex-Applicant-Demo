# Demo Mode – all data stored in browser localStorage
VITE_SUPABASE_URL=http://localhost
VITE_SUPABASE_ANON_KEY=demo
VITE_EXTERNAL_SUPABASE_URL=http://localhost
VITE_EXTERNAL_SUPABASE_ANON_KEY=demo
NODE_ENV=development
VITE_API_BASE_URL=http://localhost:3000/api
VITE_APP_NAME=CGB Applicant
VITE_DEBUG=false

# Shared local cache server (see /shared-cache-server) — lets this app
# and the Admin demo see each other's applicant/feedback/inquiry data.
VITE_SHARED_CACHE_URL=http://localhost:4500
